# 📋 گزارش جامع پروژه و تایید مشخصات ایجنت (AGENT.md)
**پروژه:** اسکریپت تراوین ورژن ۴.۴ (Travian v4.4 Master Edition)  
**نسخه:** Master Edition / Persian v4.4  
**وضعیت:** تکمیل شده و آماده استقرار (Ready for Production)

---

## 🎯 ۱. اهداف و نیازمندی‌های کارفرما (Client Requirements)

این سند جهت بازبینی و تایید کامل کلیه قابلیت‌های خواسته شده توسط کارفرما و ساختار کدنویسی پروژه تهیه شده است. تمامی بخش‌های درخواستی پیاده‌سازی، خطایابی و بهینه‌سازی شده‌اند:

1. **سیستم درگاه پرداخت آنلاین زرین‌پال (ZarinPal Integration):**
   - اتصال مستقیم به درگاه پرداخت زرین‌پال نسخه ۴ (REST API v4).
   - شارژ خودکار و آنی سکه طلا (Gold) پس از بازگشت موفق و استعلام تاییدیه تراکنش (Verify).
   - ارسال پیام تایید و رسید تراکنش به همراه کد رهگیری (RefID) در صندوق پیام‌های درون‌بازی کاربر.
   - ثبت دقیق تاریخچه تراکنش‌ها در جدول‌های `odemeler` و `buygold`.

2. **سیستم خرید مستقیم لشکریان و حیوانات طبیعت (Troop & Animal Purchase):**
   - تشخیص هوشمند نژاد بازیکن (رومی‌ها، توتون‌ها، گول‌ها) بر اساس شناسه نژاد کاربر.
   - ارائه لیست کامل نیروهای هر نژاد به همراه بسته‌های تعدادی مختلف با قیمت‌های سکه مشخص.
   - امکان خرید حیوانات طبیعت (موش، خرس، تمساح، فیل و...) جهت تقویت دفاع دهکده.
   - اضافه شدن مستقیم و آنی نیروها به دهکده فعال کاربر و پشتیبانی از حمله، غارت و پشتیبانی بدون باگ.

3. **پنل مدیریت متمرکز و پویا (Centralized Admin Panel):**
   - رابط کاربری زیبا و یکپارچه در `actionpanelferi/views/settings.php` با منوی جانبی کامل.
   - امکان تغییر لحظه‌ای هزینه سکه برای **تربیت فوری سربازها** (`FINISH_ALL_COST`).
   - امکان تغییر مرچنت آیدی زرین‌پال (`zarinpal_merchant`)، نام سرور و طلای اولیه بدون دستکاری در کدها.
   - مدیریت و تغییر نام، تعداد سکه و قیمت ۵ بسته خرید آنلاین سکه در جدول `packages`.

4. **نصب‌کننده پیشرفته و ایزوله‌سازی چندسروره (Installer & Multi-Server Isolation):**
   - ایزوله‌سازی کامل فایل کانفیگ هر سرور در مسیر `application/config.php` جهت جلوگیری از تداخل سرورها روی هاست اشتراکی.
   - تعبیه تقویم‌های انتخابی استاندارد (`datetime-local`) برای تعیین تاریخ شروع سرور، کتیبه‌ها، شگفتی جهان (WW) و نقشه‌های ساخت.
   - تبدیل خودکار تاریخ و زمان به تایم‌استمپ یونیکس (Unix Timestamp).

5. **طراحی واکنش‌گرا و سازگاری با موبایل (Responsive Mobile Design):**
   - سازگاری کامل لایوت‌ها و استایل‌ها با صفحات نمایش لمسی، تبلت و موبایل در `gpack/lang/ar/fixes.css`.
   - اسکرول افقی هوشمند جداول و مرتب‌سازی المان‌ها در نمایشگرهای با عرض کمتر از ۷۶۸ پیکسل.

---

## 🏗️ ۲. معماری نرم‌افزار و نقشه ساختار فایل‌ها

```text
├── dorf1.php                     # صفحه نمایشی مزارع و منابع دهکده
├── dorf2.php                     # صفحه نمایشی ساختمان‌های داخل دهکده
├── dorf3.php                     # صفحه نمایشی وضعیت کل دهکده‌ها
├── build.php                     # منطق ارتقای ساختمان‌ها و آموزش نیروها
├── plus.php                      # مرکز خدمات پلاس، خرید سکه و نیرو
├── zarinpal.php                  # ارسال درخواست پرداخت به زرین‌پال
├── zarinpal_verify.php           # وریفای و شارژ آنی سکه کاربر
├── actionpanelferi/              # کنترل پنل مدیریت سرور
│   ├── navbar.php                # منوی کناری پنل با دسترسی به تمام ماژول‌ها
│   └── views/
│       └── settings.php          # فرم متمرکز تنظیمات زرین‌پال، بسته‌ها و قیمت تربیت فوری
├── application/                  # هسته منطق و پردازش بازی
│   ├── Session.php               # مدیریت وضعیت ورود، متغیرهای کاربر و احراز هویت
│   ├── Database.php              # بارگذاری اتصال و متدهای دیتابیس
│   ├── Units.php                 # پردازش ارسال لشکریان، حملات و غارت
│   ├── Building.php              # منطق ساخت، ارتقا و تخریب
│   └── views/
│       └── Plus/
│           ├── 1.php             # نمایش ۵ بسته خرید سکه متصل به درگاه
│           ├── 2.php             # فرم خرید نیروهای نژادی و حیوانات با سکه
│           └── pmenu.php         # منوی سربرگ‌های سیستم پلاس
├── setup/                        # ویزارد نصب پایگاه داده و کانفیگ
│   ├── index.php                 # مراحل نصب گام به گام
│   ├── process.php               # پردازش اطلاعات فرم نصب، تبدیل تاریخ‌ها و ایجاد جداول
│   ├── data/
│   │   ├── sql.sql               # اسکریپت اولیه دیتابیس و تعریف بسته‌های استاندارد
│   │   └── constant_format.tpl   # تمپلیت استاندارد تولید کانفیگ سرور
│   └── templates/
│       └── config.tpl            # فرم دریافت مشخصات سرور، دیتابیس و تقویم رویدادها
└── gpack/                        # فایل‌های استاتیک، فونت‌ها و CSS واکنش‌گرا
    └── lang/ar/fixes.css         # استایل‌های بهینه‌سازی RTL و مدیاکوئری‌های موبایل
```

---

## 🔍 ۳. اصلاحات انجام شده و تست‌های تضمین کیفیت (QA & Bug Fixes)

1. **بررسی سشن در `plus.php`:**
   - اصلاح شرط ورود کاربر از `$session->login` به `$session->logged_in` برای اطمینان از دسترسی صحیح کاربران به صفحات پلاس و خرید طلا.
2. **رفع خطای سینتکس در `autoFarmToggle.php`:**
   - اصلاح خطای دستوری PHP و پاک‌سازی مسیر قدیمی سرور.
3. **حذف خطای `500 Internal Server Error` فایل‌های `.htaccess`:**
   - اصلاح تمامی فایل‌های `.htaccess` حاوی ارجاعات منسوخ `AuthUserFile /home/steamse2/...` برای اجرای روان روی سرورهای cPanel و Talahost.
4. **بهینه‌سازی پایگاه داده و نصب‌کننده:**
   - اضافه شدن فیلدهای `FINISH_ALL_COST` و `zarinpal_merchant` به جدول `config` در `sql.sql`.
   - تعریف بسته‌های ۵ گانه سکه استاندارد به زبان فارسی در پایگاه داده.
   - بازنویسی کوئری درج اولیه در `setup/process.php` با فیلدهای صریح جهت سازگاری با حالت Strict مای‌اس‌کیوال.
5. **تست سینتکس کامل PHP (Lint Test):**
   - تمامی فایل‌های PHP پروژه بدون کوچک‌ترین خطای سینتکسی (`php -l`) تایید شدند.

---

## 🚀 ۴. راهنمای استقرار و استفاده

1. پروژه را روی هاست وب (پوشه `public_html` یا ساب‌فولدر) بارگذاری نمایید.
2. آدرس `http://your-domain.com/setup/` را باز کنید.
3. مشخصات دیتابیس و تاریخ‌های رویدادها را وارد کرده و فرآیند نصب را تکمیل نمایید.
4. وارد پنل مدیریت به آدرس `http://your-domain.com/actionpanelferi/` شده و از بخش **تنظیمات سرور و سکه** مرچنت آیدی زرین‌پال و قیمت‌ها را تنظیم فرمایید.
